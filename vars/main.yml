---
# vars file for remi
_remi_repository:
  6: "http://rpms.remirepo.net/enterprise/remi-release-6.rpm"
  7: "http://rpms.remirepo.net/enterprise/remi-release-7.rpm"

remi_repository: "{{ _remi_repository[ansible_distribution_major_version|int] | default('none') }}"

remi_repository_key: "http://rpms.remirepo.net/RPM-GPG-KEY-remi"
